<nav class="hide-on-med-and-up">
  <div class="nav-wrapper nav-container">
    <div class="brand-logo brand-logo-small"><a href="/">IT&amp;C 210</a></div>
    <% if (title !== "tutorial") { %>
    <ul class="right">
      <li><a href="#" onclick="leave()">Log Out</a></li>
      <% if (admin) { %>
      <li><a href="/logout">Un-Admin</a></li>
      <% } else { %>
      <li><a href="/login">Admin</a></li>
      <% } %>
    </ul>
    <% } %>
  </div>
</nav>

<nav class="hide-on-small-only">
  <div class="nav-wrapper nav-container">
    <div class="brand-logo"><a href="/">IT&amp;C 210</a> | <%= title %></div>
    <% if (title !== "tutorial") { %>
    <ul class="right">
      <li><a href="#" onclick="leave()">Log Out</a></li>
      <% if (admin) { %>
      <li><a href="/logout">Un-Admin</a></li>
      <% } else { %>
      <li><a href="/login">Admin</a></li>
      <% } %>
    </ul>
    <% } %>
  </div>
</nav>

